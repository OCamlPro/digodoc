; generated by drom from package skeleton 'library'

(library
  (name digodoc_lib)
  (public_name digodoc_lib)
  (wrapped true)
  (libraries meta_file ez_opam_file ez_html ez_file )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package digodoc_lib))


