; generated by drom from package skeleton 'driver'
(executable
  (name main)
  (public_name htmlize)
  (package htmlize)
  (libraries ez_subst ez_html ez_file ez_cmdliner )
  
  
  )

(ocamllex approx_lexer)

(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package htmlize))


